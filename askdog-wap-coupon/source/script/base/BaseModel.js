define(['class'], function (Class) {

    /**
     * Base model for all models.
     * Use this as a template for all future models
     *
     * Use of Class.js
     */
    var BaseModel = Class.extend({
        // Empty, you could extend all models according extend this.
    });

    return BaseModel;

});